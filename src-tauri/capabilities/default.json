{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core app/window capabilities for the LLMChef desktop shell. File, shell, and network plugins are intentionally not granted here.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default"
  ]
}
